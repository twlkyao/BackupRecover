<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twlkyao.backuprecover"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="8" />
 <!-- The permission to read sms. -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <!-- The permission to write sms. -->
    <uses-permission android:name="android.permission.WRITE_SMS" />
    <!-- The permission to read contacts. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- The permission to write contatcs. -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <!-- The permission to read and write sdcard. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Allows mounting and unmounting file systems for removable storage. -->
    <!-- <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" /> -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Black.NoTitleBar" >
        <activity
            android:name="com.twlkyao.backuprecover.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
